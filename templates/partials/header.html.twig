<header class="site-header p-3 mb-3 bg-light border-bottom">
    {# Use a flexbox container to align items #}
    <div class="header-inner d-flex justify-content-between align-items-center mx-auto" style="max-width: 900px;">

        {# Main Logo/Title #}
        <h1 class="logo fs-4 fw-bold mb-0">Sarah's MenuMind</h1>

        {# Navigation Link #}
        <nav>
            <a href="{{ path('app_landing') }}" class="text-decoration-none text-dark fw-semibold">About</a>
        </nav>
    </div>
</header>
